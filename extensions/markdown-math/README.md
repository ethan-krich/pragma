# Markdown Math

**Notice:** This extension is bundled with Pragma. It can be disabled but not uninstalled.

Adds math rendering using [KaTeX](https://katex.org) to Pragma's built-in markdown preview and markdown cells in notebooks.
